# 灵山胜境 AI 数字人导览系统开发任务切片

**版本**：v2.0  
**日期**：2026-05-10  
**依据文档**：
- `docs/灵山胜境AI数字人导览系统PRD-2026-05-10.md`
- `docs/项目当前状态评估报告-2026-05-09.md`
- 当前 `OtakuClaw` 项目状态

---

## 1. 文档目的

本文档基于当前项目完成度（55-60%），将剩余开发任务拆解为可执行的开发任务切片。

**当前已完成**（✅）：
- 官方资料导入器（95%）
- 知识库存储（90%）
- RAG检索（85%）
- 游客端UI基础（70%）
- 管理端UI基础（60%）

**仍需完成**（❌）：
- 交互日志系统
- 游客行为分析服务
- 路线推荐UI
- 数据大屏
- 评测中心
- 数字人情感状态
- 满意度反馈
- 流式TTS集成

---

## 2. 当前项目完成度总览

### 2.1 已完成的模块（可复用）

| 模块 | 完成度 | 文件路径 | 状态 |
|------|--------|----------|------|
| Scenic Guide服务目录 | 100% | `desktop/electron/services/scenicGuide/` | ✅ |
| OfficialDataManifestStore | 100% | `officialDataManifestStore.js` | ✅ |
| OfficialDataImporter | 95% | `officialDataImporter.js` | ✅ |
| DocxTextExtractor | 100% | `docxTextExtractor.js` | ✅ |
| SpotDatasetParser | 100% | `spotDatasetParser.js` | ✅ |
| GuideDocParser | 100% | `guideDocParser.js` | ✅ |
| BehaviorDatasetStore | 100% | `behaviorDatasetStore.js` | ✅ |
| ScenicKnowledgeStore | 90% | `scenicKnowledgeStore.js` | ✅ |
| ScenicSearchIndex | 85% | `scenicSearchIndex.js` | ✅ |
| ScenicRagService | 85% | `scenicRagService.js` | ✅ |
| ScenicGuideShell | 70% | `front_end/src/shells/ScenicGuideShell.jsx` | ✅ |
| ScenicAdminShell | 60% | `front_end/src/shells/ScenicAdminShell.jsx` | ✅ |
| IPC接口 | 100% | `desktop/electron/ipc/scenicGuide.js` | ✅ |

### 2.2 缺失的模块（需新建）

| 模块 | 优先级 | 预计工作量 |
|------|--------|------------|
| InteractionLogStore | P0 | 2天 |
| VisitorAnalyticsService | P0 | 2天 |
| RoutePlannerService | P0 | 3天 |
| ScenicEvalService | P0 | 2天 |
| AnalyticsDashboard | P0 | 2天 |
| ScenicBigScreen | P0 | 2天 |
| EvalCenter | P0 | 2天 |
| RoutePlannerPanel | P1 | 2天 |
| RouteResultCard | P1 | 1天 |
| KnowledgeManager | P1 | 2天 |
| AnswerFeedback | P1 | 1天 |
| GuideStage | P1 | 2天 |
| 流式TTS集成 | P1 | 3天 |

---

## 3. 4周开发冲刺计划

### Week 1：P0基础服务（Day 1-7）

**目标**：完成交互日志和行为分析服务

#### 开发者A（后端）

**Day 1-2：InteractionLogStore**
```javascript
// 文件：desktop/electron/services/scenicGuide/interactionLogStore.js

class InteractionLogStore {
  // 保存单次问答日志
  saveLog(logData) {
    // logData: {
    //   timestamp, inputType, question, intent,
    //   sources, answer, latency: {asr, rag, llm, tts},
    //   rating, emotion, unmatched
    // }
  }
  
  // 查询日志（支持分页和筛选）
  queryLogs(filters) {}
  
  // 获取统计数据
  getStatistics(timeRange) {}
}
```

验收标准：
- 可记录完整问答链路
- 支持按时间、未命中、差评筛选
- 延迟数据可结构化存储

**Day 3-4：VisitorAnalyticsService**
```javascript
// 文件：desktop/electron/services/scenicGuide/visitorAnalyticsService.js

class VisitorAnalyticsService {
  // 获取大屏数据
  getDashboardData() {
    return {
      todayServiceCount, voiceQaCount, routeRecommendCount,
      hotQuestions, hotSpots, satisfactionTrend,
      hitRate, avgLatency, officialDataCompleteness
    };
  }
  
  // 获取游客画像（基于Excel数据）
  getVisitorPortrait() {
    return { age, gender, consumption, stayDuration, satisfaction };
  }
}
```

验收标准：
- 聚合交互日志生成统计
- 分析Excel行为数据
- 生成大屏所需数据结构

**Day 5：IPC接口暴露**
```javascript
// desktop/electron/ipc/scenicGuide.js 新增
ipcMain.handle('scenic-guide:get-interaction-logs', ...);
ipcMain.handle('scenic-guide:get-analytics-dashboard', ...);
ipcMain.handle('scenic-guide:get-visitor-portrait', ...);
```

**Day 6-7：缓冲时间与联调**

#### 开发者B（前端）

**Day 1-2：RoutePlannerPanel（偏好选择）**
```jsx
// front_end/src/components/scenic/RoutePlannerPanel.jsx

<FormGroup>
  <FormLabel>兴趣偏好</FormLabel>
  <FormGroup row>
    <FormControlLabel control={<Checkbox />} label="历史文化" />
    <FormControlLabel control={<Checkbox />} label="自然风光" />
    <FormControlLabel control={<Checkbox />} label="亲子互动" />
  </FormGroup>
  
  <FormLabel>游览时长</FormLabel>
  <ToggleButtonGroup>
    <ToggleButton value="1h">1小时</ToggleButton>
    <ToggleButton value="2h">2小时</ToggleButton>
    <ToggleButton value="half">半日</ToggleButton>
    <ToggleButton value="full">一日</ToggleButton>
  </ToggleButtonGroup>
  
  {/* 人群、体力、特殊需求... */}
  
  <Button variant="contained" onClick={handleGenerate}>
    生成推荐路线
  </Button>
</FormGroup>
```

**Day 3-4：RouteResultCard（路线结果卡片）**
```jsx
// front_end/src/components/scenic/RouteResultCard.jsx

<Card>
  <CardHeader
    title="历史文化深度游（2小时版）"
    subheader="适合：历史文化爱好者 | 预计：2小时"
  />
  <CardContent>
    <Typography variant="h6">推荐理由</Typography>
    <List>
      <ListItem>✓ 您选择了"历史文化"兴趣偏好</ListItem>
      <ListItem>✓ 官方路线匹配度 92%</ListItem>
      <ListItem>✓ 适合上午9点开始，避开人流高峰</ListItem>
    </List>
    
    <Typography variant="h6">行程安排</Typography>
    <Timeline>
      <TimelineItem>09:00 灵山大佛（90分钟）</TimelineItem>
      <TimelineItem>10:30 祥符禅寺（30分钟）</TimelineItem>
    </Timeline>
    
    <Chip label="来源：官方游览指南" size="small" />
  </CardContent>
</Card>
```

**Day 5：集成到ScenicGuideShell**
- 新增"路线推荐"标签页
- 路线面板弹出/收起动画

**Day 6-7：缓冲时间与Mock数据准备**

---

### Week 2：P0核心功能（Day 8-14）⭐关键周

**目标**：完成路线推荐、数据大屏、评测中心

#### 开发者A（后端）

**Day 8-9：RoutePlannerService**
```javascript
// 文件：desktop/electron/services/scenicGuide/routePlannerService.js

class RoutePlannerService {
  // 根据偏好生成路线推荐
  planRoute(preferences) {
    const { interests, duration, crowd, stamina } = preferences;
    
    // 1. 匹配官方路线
    const officialRoutes = this.matchOfficialRoutes(interests);
    
    // 2. 时长裁剪
    const route = duration < this.getOfficialDuration(officialRoutes[0])
      ? this.trimRoute(officialRoutes[0], duration)
      : officialRoutes[0];
    
    // 3. 生成推荐理由
    route.reason = this.generateReason(route, preferences);
    
    return route;
  }
  
  // 匹配官方路线
  matchOfficialRoutes(interests) {
    const routeMap = {
      '历史文化': '历史文化爱好者路线',
      '自然风光': '自然风光爱好者路线',
      '亲子': '亲子家庭路线'
    };
    return interests.map(i => routeMap[i]).filter(Boolean);
  }
}
```

验收标准：
- 历史文化偏好匹配历史文化路线
- 2小时路线说明裁剪来源
- 推荐理由可解释

**Day 10-11：ScenicEvalService**
```javascript
// 文件：desktop/electron/services/scenicGuide/scenicEvalService.js

class ScenicEvalService {
  // 运行评测
  async runEvaluation(questionSet) {
    const results = [];
    
    for (const q of questionSet) {
      const start = Date.now();
      const answer = await scenicRagService.askQuestion({ question: q.question });
      const end = Date.now();
      
      results.push({
        question: q.question,
        category: q.category,
        expected: q.expected,
        actual: answer.answer,
        sources: answer.sources,
        latency: end - start,
        hit: this.checkHit(answer, q.expected),
        noHitAllowed: q.noHitAllowed,
        noHitCorrect: answer.status === 'no_hit'
      });
    }
    
    return this.generateReport(results);
  }
  
  // 生成报告
  generateReport(results) {
    return {
      totalAccuracy: this.calculateAccuracy(results),
      noHitRefusalRate: this.calculateNoHitRefusal(results),
      sourceCompleteness: this.calculateSourceCompleteness(results),
      latency: this.calculateLatency(results),
      categoryAccuracy: this.calculateByCategory(results)
    };
  }
}
```

**Day 12：评测题集编写**
```json
// docs/scenic-demo/eval/lingshan-questions.json

{
  "questions": [
    {
      "id": "LS-001-01",
      "question": "灵山大佛有什么特色？",
      "category": "点位事实",
      "expected": ["88米", "青铜", "佛体", "莲花座"],
      "prohibited": ["99米", "黄金"],
      "source": "LS-001",
      "difficulty": "easy"
    },
    // ... 共100题
  ]
}
```

题集分布：
- 灵山胜境点位事实：30题
- 拈花湾点位事实：12题
- 历史文化与佛教文化：23题
- 核心景点讲解：15题
- 官方路线推荐：10题
- 门票与贴士：10题

**Day 13-14：IPC暴露与联调**

#### 开发者B（前端）

**Day 8-9：AnalyticsDashboard（管理页分析视图）**
```jsx
// front_end/src/components/scenic/AnalyticsDashboard.jsx

<Grid container spacing={2}>
  {/* 核心指标卡片 */}
  <Grid item xs={3}>
    <StatCard title="今日服务人次" value={1,247} icon={<People />} />
  </Grid>
  <Grid item xs={3}>
    <StatCard title="语音问答次数" value={856} icon={<Mic />} />
  </Grid>
  <Grid item xs={3}>
    <StatCard title="满意度" value="4.6/5" icon={<Star />} />
  </Grid>
  <Grid item xs={3}>
    <StatCard title="知识库命中率" value="87.5%" icon={<CheckCircle />} />
  </Grid>
  
  {/* ECharts图表 */}
  <Grid item xs={6}>
    <BarChart title="热门问题Top10" data={hotQuestions} />
  </Grid>
  <Grid item xs={6}>
    <PieChart title="游客画像" data={visitorPortrait} />
  </Grid>
  <Grid item xs={12}>
    <LineChart title="满意度趋势" data={satisfactionTrend} />
  </Grid>
</Grid>
```

**Day 10-11：ScenicBigScreen（投影大屏）**
```jsx
// front_end/src/components/scenic/ScenicBigScreen.jsx

<Box className="scenic-big-screen" sx={{ width: 1920, height: 1080 }}>
  {/* 头部 */}
  <Typography variant="h2">
    灵山胜境AI数字人导览系统 - 实时运营监控大屏
  </Typography>
  
  <Grid container spacing={2}>
    {/* 左侧：服务统计 */}
    <Grid item xs={3}>
      <Paper className="big-screen-panel">
        <Typography variant="h4">今日服务</Typography>
        <Typography variant="h1">1,247人次</Typography>
        <Divider />
        <Typography>语音问答：856次</Typography>
        <Typography>路线推荐：124次</Typography>
      </Paper>
    </Grid>
    
    {/* 中间：延迟监控 */}
    <Grid item xs={6}>
      <Paper className="big-screen-panel">
        <Typography variant="h4">实时问答延迟监控</Typography>
        <LineChart data={latencyHistory} />
        <Typography>
          平均: 1.8s | 最大: 4.2s | 目标: &lt;5s
        </Typography>
      </Paper>
    </Grid>
    
    {/* 右侧：游客画像 */}
    <Grid item xs={3}>
      <Paper className="big-screen-panel">
        <Typography variant="h4">游客画像分布</Typography>
        <RadarChart data={visitorPortrait} />
        <Typography>消费结构：票务58% | 餐饮25% | 购物12%</Typography>
      </Paper>
    </Grid>
    
    {/* 底部：核心指标 */}
    <Grid item xs={12}>
      <Box className="big-screen-metrics">
        <MetricCard label="官方点位" value="22个✅" />
        <MetricCard label="行为记录" value="14万+✅" />
        <MetricCard label="评测准确率" value="92.3%✅" />
        <MetricCard label="满意度" value="4.6/5" />
      </Box>
    </Grid>
  </Grid>
</Box>
```

**Day 12-13：EvalCenter（评测中心）**
```jsx
// front_end/src/components/scenic/EvalCenter.jsx

<Box>
  <Box className="eval-controls">
    <Button variant="contained" onClick={handleRunEval}>
      运行评测（100题）
    </Button>
    <Button onClick={handleExportReport}>导出报告</Button>
  </Box>
  
  {/* 进度 */}
  {evaluating && (
    <LinearProgress variant="determinate" value={progress} />
  )}
  
  {/* 结果 */}
  <Box className="eval-results">
    <Grid container spacing={2}>
      <Grid item xs={3}>
        <ResultCard label="总准确率" value="92.3%" status="success" />
      </Grid>
      <Grid item xs={3}>
        <ResultCard label="未命中拒答率" value="96.7%" status="success" />
      </Grid>
      <Grid item xs={3}>
        <ResultCard label="来源完整率" value="98.0%" status="success" />
      </Grid>
      <Grid item xs={3}>
        <ResultCard label="平均延迟" value="2.0s" status="success" />
      </Grid>
    </Grid>
    
    {/* 分类准确率表格 */}
    <Table>
      <TableHead>
        <TableRow>
          <TableCell>类别</TableCell>
          <TableCell align="right">准确率</TableCell>
          <TableCell align="right">通过/总数</TableCell>
        </TableRow>
      </TableHead>
      <TableBody>
        <TableRow>
          <TableCell>点位事实</TableCell>
          <TableCell align="right">93.3%</TableCell>
          <TableCell align="right">28/30</TableCell>
        </TableRow>
        {/* ... */}
      </TableBody>
    </Table>
    
    {/* 错题列表 */}
    <Box className="wrong-answers">
      <Typography variant="h6">错题分析</Typography>
      <List>
        <ListItem>
          <ListItemText
            primary="灵山梵宫的建筑风格是什么？"
            secondary="回答不完整 | 原因：知识块分割过细"
          />
        </ListItem>
      </List>
    </Box>
  </Box>
</Box>
```

**Day 14：集成与联调**

---

### Week 3：P1完善功能（Day 15-21）

**目标**：知识库管理、满意度反馈、数字人状态

#### 开发者A（后端）

**Day 15-16：KnowledgeManager后端**
```javascript
// desktop/electron/ipc/scenicGuide.js 新增

ipcMain.handle('scenic-guide:create-faq', async (_event, data) => {
  const faq = {
    id: generateId(),
    question: data.question,
    answer: data.answer,
    source: 'manual', // official | manual | derived
    published: true,
    createdAt: Date.now()
  };
  scenicKnowledgeStore.addFaq(faq);
  return { ok: true, faq };
});

ipcMain.handle('scenic-guide:update-faq', async (_event, id, data) => {
  scenicKnowledgeStore.updateFaq(id, data);
  return { ok: true };
});

ipcMain.handle('scenic-guide:delete-faq', async (_event, id) => {
  scenicKnowledgeStore.deleteFaq(id);
  return { ok: true };
});
```

**Day 17：反馈数据聚合**
```javascript
// desktop/electron/services/scenicGuide/interactionLogStore.js 新增

getFeedbackSummary(timeRange) {
  return {
    total: this.logs.length,
    positive: this.logs.filter(l => l.rating === 'up').length,
    negative: this.logs.filter(l => l.rating === 'down').length,
    satisfactionRate: /* 计算 */,
    negativeReasons: this.groupNegativeReasons(),
    unmatchedQuestions: this.getUnmatchedQuestions()
  };
}
```

**Day 18：导游Prompt优化**
```javascript
// desktop/electron/services/scenicGuide/scenicGuidePrompt.js 优化

const SCENIC_GUIDE_PROMPT = `
你是灵山胜境的AI导游，负责为游客提供专业、准确的景区讲解。

## 核心原则
1. 所有回答必须基于官方提供的资料，严禁编造
2. 未收录的信息应明确告知，不可猜测
3. 回答简洁明了，适合语音播报
4. 对佛教文化保持尊重、庄重的语气

## 回答策略
### 景点讲解
- 先介绍核心特色（1-2句）
- 再补充背景信息
- 最后给出游览建议

### 路线推荐
- 说明推荐理由
- 列出游览顺序
- 标注每个点位的预计时间
- 说明来源（官方路线名称）

### 未命中处理
- 标准回复："官方资料中暂未收录此信息，建议以景区当日公告或游客中心答复为准。"
- 记录问题供后续补充

## 情感表达
- 讲述佛教文化：使用恭敬、庄重的语气
- 讲述自然风光：使用轻松、愉快的语气
- 讲述建筑艺术：使用专业、严谨的语气
- 未命中/道歉：使用歉意、诚恳的语气
`;
```

**Day 19：性能优化**
- RAG检索缓存
- 索引预热
- 延迟监控完善

**Day 20-21：缓冲时间**

#### 开发者B（前端）

**Day 15-16：KnowledgeManager**
```jsx
// front_end/src/components/scenic/KnowledgeManager.jsx

<Box>
  {/* 筛选器 */}
  <Box className="knowledge-filter">
    <TextField placeholder="搜索知识块..." />
    <Select value={sourceFilter} label="来源">
      <MenuItem value="all">全部</MenuItem>
      <MenuItem value="official">官方原文</MenuItem>
      <MenuItem value="manual">人工修订</MenuItem>
    </Select>
    <Select value={typeFilter} label="类型">
      <MenuItem value="spot">景点</MenuItem>
      <MenuItem value="route">路线</MenuItem>
      <MenuItem value="faq">FAQ</MenuItem>
    </Select>
  </Box>
  
  {/* 知识库列表 */}
  <Table>
    <TableHead>
      <TableRow>
        <TableCell>标题</TableCell>
        <TableCell>来源</TableCell>
        <TableCell>类型</TableCell>
        <TableCell>状态</TableCell>
        <TableCell>操作</TableCell>
      </TableRow>
    </TableHead>
    <TableBody>
      {knowledgeBlocks.map(block => (
        <TableRow key={block.id}>
          <TableCell>{block.title}</TableCell>
          <TableCell>{block.source}</TableCell>
          <TableCell>{block.type}</TableCell>
          <TableCell>
            <Chip 
              label={block.published ? '已发布' : '草稿'} 
              color={block.published ? 'success' : 'default'}
            />
          </TableCell>
          <TableCell>
            <Button onClick={() => handleEdit(block)}>编辑</Button>
            <Button onClick={() => handleDelete(block.id)}>删除</Button>
          </TableCell>
        </TableRow>
      ))}
    </TableBody>
  </Table>
  
  {/* FAQ新增 */}
  <Dialog open={faqDialogOpen} onClose={() => setFaqDialogOpen(false)}>
    <DialogTitle>新增FAQ</DialogTitle>
    <DialogContent>
      <TextField label="问题" fullWidth />
      <TextField label="回答" fullWidth multiline />
    </DialogContent>
    <DialogActions>
      <Button onClick={handleSaveFaq}>保存</Button>
      <Button onClick={() => setFaqDialogOpen(false)}>取消</Button>
    </DialogActions>
  </Dialog>
</Box>
```

**Day 17：AnswerFeedback**
```jsx
// front_end/src/components/scenic/AnswerFeedback.jsx

<Box className="answer-feedback">
  <Typography variant="caption">这个回答有帮助吗？</Typography>
  <Box className="feedback-actions">
    <IconButton onClick={() => handleRate('up')}>
      <ThumbUpIcon color={rating === 'up' ? 'primary' : 'disabled'} />
    </IconButton>
    <IconButton onClick={() => handleRate('down')}>
      <ThumbDownIcon color={rating === 'down' ? 'error' : 'disabled'} />
    </IconButton>
  </Box>
  
  {/* 快捷反馈 */}
  {rating === 'down' && (
    <Box className="feedback-reasons">
      <Chip label="太长了" onClick={() => handleReason('too_long')} />
      <Chip label="没听懂" onClick={() => handleReason('unclear')} />
      <Chip label="不准确" onClick={() => handleReason('inaccurate')} />
    </Box>
  )}
</Box>
```

**Day 18-19：GuideStage（数字人舞台）**
```jsx
// front_end/src/components/scenic/GuideStage.jsx

<Box className="guide-stage">
  {/* Live2D模型 */}
  <Live2DViewer
    modelFile={modelFile}
    motion={currentMotion}
    expression={currentExpression}
  />
  
  {/* 状态指示器 */}
  <Box className="guide-status">
    <Chip 
      icon={<MicIcon />}
      label={statusText}
      color={statusColor}
    />
  </Box>
</Box>

// 状态管理
const DIGITAL_HUMAN_STATES = {
  IDLE: { motion: 'idle', expression: 'neutral', label: '待机' },
  LISTENING: { motion: 'nodding', expression: 'attentive', label: '聆听中' },
  THINKING: { motion: 'thinking', expression: 'focused', label: '思考中' },
  SPEAKING: { motion: 'speaking', expression: 'dynamic', label: '讲解中' },
  GUIDING: { motion: 'pointing', expression: 'enthusiastic', label: '推荐路线' },
  APOLOGIZING: { motion: 'bowing', expression: 'regretful', label: '抱歉' },
  HAPPY: { motion: 'celebrating', expression: 'joyful', label: '开心' },
};
```

**Day 20：数字人状态桥接**
```jsx
// front_end/src/hooks/scenic/useDigitalHumanState.js

export function useDigitalHumanState() {
  const [state, setState] = useState('IDLE');
  
  useEffect(() => {
    // 监听问答状态变化
    const handleQuestionStart = () => setState('LISTENING');
    const handleRagStart = () => setState('THINKING');
    const handleTtsStart = () => setState('SPEAKING');
    const handleTtsEnd = () => setState('IDLE');
    const handleRouteShow = () => setState('GUIDING');
    const handleNoHit = () => setState('APOLOGIZING');
    const handlePositiveRating = () => {
      setState('HAPPY');
      setTimeout(() => setState('IDLE'), 2000);
    };
    
    // 注册事件监听
    // ...
    
    return () => {
      // 清理监听
    };
  }, []);
  
  return DIGITAL_HUMAN_STATES[state];
}
```

**Day 21：缓冲时间**

---

### Week 4：集成测试与交付（Day 22-28）

**Day 22-23：端到端测试**
```
测试清单：
□ 官方资料导入完整流程
□ 文本问答（20轮不中断）
□ 语音问答（10轮不中断）
□ 路线推荐（5次不卡死）
□ 未命中问题正确拒答
□ 满意度反馈记录
□ 交互日志查看
□ 数据大屏展示
□ 评测中心运行

性能验证：
□ 文本首句 < 2秒
□ 语音完整链路 < 5秒
□ 准确率 ≥ 90%
```

**Day 24：演示数据准备**
- 准备官方资料包
- 准备演示问答脚本
- 清理演示日志

**Day 25：演示脚本+录屏**
- 编写10分钟演示流程
- 录制核心功能演示
- 准备离线兜底方案

**Day 26：文档更新**
- 更新README.md
- 更新技术架构图
- 更新许可说明

**Day 27：打包部署**
- Electron打包配置
- 安装包制作
- 演示机器部署测试

**Day 28：最终验收**

---

## 4. 任务优先级定义

| 优先级 | 定义 | 示例 |
|--------|------|------|
| P0 | 比赛必须有，没有就不成立 | 交互日志、数据大屏、评测中心 |
| P1 | 高奖强相关，建议进入主版本 | 数字人状态、满意度反馈、知识库管理 |
| P2 | 锦上添花，有余力再做 | 拍照问导游、多模态能力 |

---

## 5. 任务规模定义

| 规模 | 预计时间 | 示例 |
|------|----------|------|
| S | 0.5天 | 简单工具函数、UI微调 |
| M | 1-2天 | 单一服务模块、简单组件 |
| L | 3-5天 | 跨主进程/前端/测试 |
| XL | 1周+ | 需要拆分的复杂功能 |

---

## 6. 缺失组件详细任务

### 6.1 InteractionLogStore（P0，2天）

**文件**：`desktop/electron/services/scenicGuide/interactionLogStore.js`

**数据结构**：
```javascript
{
  id: string,
  timestamp: number,
  inputType: 'text' | 'voice',
  question: string,
  intent: 'fact' | 'route' | 'practical',
  sources: Array<{
    blockId: string,
    title: string,
    excerpt: string
  }>,
  answer: string,
  latency: {
    asr: number,
    rag: number,
    llmFirstToken: number,
    llmComplete: number,
    ttsFirstAudio: number,
    complete: number
  },
  rating: 'up' | 'down' | null,
  emotion: string | null,
  unmatched: boolean
}
```

**验收**：
- 可按时间分页查询
- 可按未命中/差评筛选
- 延迟数据可聚合

### 6.2 VisitorAnalyticsService（P0，2天）

**文件**：`desktop/electron/services/scenicGuide/visitorAnalyticsService.js`

**功能**：
- 聚合交互日志统计
- 分析Excel行为数据
- 生成大屏数据结构

**验收**：
- 可返回热门问题Top10
- 可返回热门景点Top5
- 可返回满意度趋势
- 可返回游客画像分布

### 6.3 RoutePlannerService（P0，3天）

**文件**：`desktop/electron/services/scenicGuide/routePlannerService.js`

**功能**：
- 匹配官方路线
- 时长裁剪
- 生成推荐理由

**验收**：
- 历史文化偏好匹配历史文化路线
- 2小时路线说明裁剪来源
- 推荐理由可解释

### 6.4 ScenicEvalService（P0，2天）

**文件**：`desktop/electron/services/scenicGuide/scenicEvalService.js`

**功能**：
- 批量运行评测题
- 生成准确率报告
- 生成延迟报告

**验收**：
- 可输出总准确率
- 可输出类别准确率
- 可输出未命中拒答正确率
- 可输出来源完整率

### 6.5 AnalyticsDashboard（P0，2天）

**文件**：`front_end/src/components/scenic/AnalyticsDashboard.jsx`

**依赖**：ECharts或Recharts

**验收**：
- 至少显示6个核心指标
- 图表不出现空白
- 指标口径有说明

### 6.6 ScenicBigScreen（P0，2天）

**文件**：`front_end/src/components/scenic/ScenicBigScreen.jsx`

**布局**：1920x1080投影优化

**验收**：
- 适合投影展示
- 信息层级清楚
- 深色主题优化

### 6.7 EvalCenter（P0，2天）

**文件**：`front_end/src/components/scenic/EvalCenter.jsx`

**功能**：
- 运行评测
- 展示结果
- 导出报告

**验收**：
- 可运行100题评测
- 可展示准确率报告
- 可展示延迟报告
- 可展示错题分析

### 6.8 RoutePlannerPanel（P1，2天）

**文件**：`front_end/src/components/scenic/RoutePlannerPanel.jsx`

**功能**：
- 偏好选择表单
- 生成推荐按钮
- 加载状态

**验收**：
- 偏好选项完整
- 表单验证正确
- 加载状态可见

### 6.9 RouteResultCard（P1，1天）

**文件**：`front_end/src/components/scenic/RouteResultCard.jsx`

**功能**：
- 路线信息展示
- 点位顺序列表
- 推荐理由
- 来源说明

**验收**：
- 卡片信息完整
- 窄屏不溢出

### 6.10 KnowledgeManager（P1，2天）

**文件**：`front_end/src/components/scenic/KnowledgeManager.jsx`

**功能**：
- 知识库列表
- 筛选器
- 检索测试
- FAQ编辑

**验收**：
- 可查到LS-011灵山大佛
- 检索测试显示命中来源
- FAQ发布后游客端可命中

### 6.11 AnswerFeedback（P1，1天）

**文件**：`front_end/src/components/scenic/AnswerFeedback.jsx`

**功能**：
- 点赞点踩
- 快捷原因
- 写回日志

**验收**：
- 管理端能看到差评
- "不准确"进入待检查列表

### 6.12 GuideStage（P1，2天）

**文件**：`front_end/src/components/scenic/GuideStage.jsx`

**功能**：
- Live2D展示
- 状态指示
- 动画控制

**验收**：
- TTS前后状态可切换
- 无模型时页面仍可演示问答

### 6.13 流式TTS集成（P1，3天）

**文件**：
- `desktop/electron/services/voice/streamingTtsService.js`
- `front_end/src/hooks/voice/useStreamingTts.js`

**技术方案**：
1. 监听LLM的text-delta事件
2. 断句检测（按标点符号）
3. 逐句TTS合成
4. 音频队列播放
5. 同步数字人口型

**验收**：
- LLM吐出首句立即触发TTS
- TTS合成出第一段音频立即播放
- 数字人口型同步
- 完整链路<5秒

---

## 7. 关键检查点

### Week 1 检查点
- [ ] 交互日志可记录
- [ ] 行为数据可统计
- [ ] 路线面板UI可用（Mock数据）

### Week 2 检查点 ⭐关键周
- [ ] ✅ 路线推荐可真实运行
- [ ] ✅ 数据大屏可真实展示
- [ ] ✅ 评测中心可生成报告
- [ ] ✅ 核心演示流程可跑通

### Week 3 检查点
- [ ] 知识库可管理
- [ ] 满意度可反馈
- [ ] 数字人有状态表现

### Week 4 检查点
- [ ] 端到端测试通过
- [ ] 演示脚本完成
- [ ] 打包部署成功

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| Week 2进度滞后 | 核心功能无法演示 | 提前开始、加班赶工、降级P1功能 |
| TTS延迟超标 | <5秒指标不达标 | 流式TTS集成、索引预热 |
| 准确率不达标 | ≥90%指标不达标 | 优化Prompt、补充知识块 |
| 现场演示失败 | 比赛得分低 | 准备录屏+离线兜底 |

---

## 9. 全局Definition of Done

任一任务完成必须满足：

1. **功能完整**：有明确用户可见或开发可验证的产出
2. **安全合规**：不破坏安全边界，不暴露密钥给Renderer
3. **数据完整**：官方资料不硬编码路径，每条知识有来源
4. **测试覆盖**：新增服务有单元测试或明确无法测试说明
5. **UI适配**：前端页面在窄屏下不出现明显重叠
6. **文案正确**：围绕灵山胜境导览，不出现旧项目术语
7. **代码提交**：任务相关文件提交到git，原始官方资料不误提交

---

**文档版本**：v2.0  
**最后更新**：2026-05-10  
**更新说明**：
- 基于当前项目完成度（55-60%）重新切片
- 明确Week 1-4开发计划和检查点
- 细化13个缺失组件的实现要求
- 强调低延迟和高准确率的攻坚策略
