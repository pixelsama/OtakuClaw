# 最近三次提交实机回归报告（2026-03-14）

## 背景

本报告用于记录 `main` 最近 3 个功能提交在桌面端的实机回归结果，验证引导流程、配置面板与更新入口是否与预期一致。

## 覆盖提交

- `75f7af2 feat: start roadmap UX updates for onboarding and nanobot settings`
- `dae2fc0 feat: implement voice onboarding staged flow and updater M1 IPC`
- `e765612 feat: add updater controls to preferences panel`

## 测试环境

- 日期：2026-03-14
- 系统：macOS
- 运行方式：`desktop:dev`（Renderer + Electron），CDP 端口 `9222`
- 数据状态：执行前已清理 `~/Library/Application Support/otakuclaw-desktop`、偏好设置和 keychain 相关 secret

## 回归范围

- 首次引导：语言 -> 推理后端 -> ASR -> TTS 全流程
- Nanobot：Provider 控件形态、选项清单、API Key 掩码输入
- 语音引导：ASR/TTS 三段式子步骤（来源与模型 -> 配置 -> 测试）与跨步骤反馈隔离
- 配置面板：聊天后端中的 Nanobot Provider 控件形态
- 偏好设置：Updater 区块、按钮、状态文案与 dev 模式提示

## 结果总览

- 检查点总数：50
- 通过：50
- 失败：0
- 结论：本轮覆盖范围内未发现阻断或回归问题。

## 关键验证结论

- Nanobot Provider 在首次引导与配置面板中均为下拉，不再是自由输入。
- Nanobot API Key 与 DashScope API Key 输入框均为 `password` 掩码类型。
- ASR/TTS 引导三段式流程可正常推进，云端来源下模型可从下拉选择。
- 后端连接反馈不会串扰到 ASR/TTS 步骤。
- 偏好设置的 Updater 区块可见；在 dev 环境下显示“未打包应用不可更新”提示，按钮可见且符合预期禁用状态。

## 证据产物

- 报告：`/tmp/openclaw-gui-test/latest3-commit-gui-report-1773476650972.json`
- 自动化日志：`/tmp/openclaw-gui-test/cdp_latest3_commit_retest_20260314.log`

## 备注

- 自动更新完整链路（下载与安装）必须在打包后的 `.app/.dmg` 环境验证；`desktop:dev` 仅验证 UI、状态机与错误提示路径。
